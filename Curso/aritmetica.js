const [a, b, c, d] = [2, 5, 7, 8]
const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = c * d
const divisao = d / a
const modulo = b % 3

console.log(soma, subtracao, multiplicacao, divisao, modulo)

